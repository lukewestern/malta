# Required
version: 2

# Image to use
build:
  os: ubuntu-lts-latest
  tools:
    python: "3.9"

# Configuration
python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
